import 'package:flutter/material.dart';

class ReelsCamera extends StatelessWidget {
  const ReelsCamera({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
